

enum directory {
  current = 0, recursive = 1
};

int main(int argc, char* argv[]) {
  
  int flag;
  switch(flag) {
    case current:
      // Search the current directory normally
    case recursive:
      // Search the current directory recursively
        
    default:
      // Search the current directory normally
  }
  return 0;
}